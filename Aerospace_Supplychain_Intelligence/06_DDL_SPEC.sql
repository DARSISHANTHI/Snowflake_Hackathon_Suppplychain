-- DDL for AEROSPACE_SUPPLY_CHAIN.RAW schema tables
-- Source: AEROSPACE_SUPPLY_CHAIN.GOLD (schema renamed to RAW)

CREATE SCHEMA IF NOT EXISTS AEROSPACE_SUPPLY_CHAIN.RAW;

create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_BOM (
	BOM_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	BOM_ID VARCHAR(20) NOT NULL,
	BOM_CODE VARCHAR(30) NOT NULL,
	PARENT_PART_ID VARCHAR(20) NOT NULL,
	CHILD_PART_ID VARCHAR(20) NOT NULL,
	QUANTITY_PER NUMBER(10,4) NOT NULL,
	POSITION_NUMBER NUMBER(38,0),
	EFFECTIVE_DATE DATE,
	EXPIRY_DATE DATE,
	BOM_LEVEL NUMBER(38,0),
	PROGRAM VARCHAR(50),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (BOM_ID),
	primary key (BOM_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_CALENDAR (
	DATE_KEY DATE NOT NULL,
	YEAR NUMBER(38,0),
	QUARTER NUMBER(38,0),
	MONTH NUMBER(38,0),
	MONTH_NAME VARCHAR(15),
	WEEK_OF_YEAR NUMBER(38,0),
	DAY_OF_WEEK NUMBER(38,0),
	DAY_NAME VARCHAR(15),
	IS_WEEKEND BOOLEAN,
	IS_HOLIDAY BOOLEAN DEFAULT FALSE,
	FISCAL_YEAR NUMBER(38,0),
	FISCAL_QUARTER NUMBER(38,0),
	primary key (DATE_KEY)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_CARRIER (
	CARRIER_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	CARRIER_ID VARCHAR(15) NOT NULL,
	CARRIER_CODE VARCHAR(30) NOT NULL,
	CARRIER_NAME VARCHAR(200) NOT NULL,
	CARRIER_TYPE VARCHAR(30),
	COUNTRY VARCHAR(50),
	ON_TIME_PCT NUMBER(5,2),
	COST_PER_KG NUMBER(10,4),
	IS_ACTIVE BOOLEAN DEFAULT TRUE,
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (CARRIER_ID),
	primary key (CARRIER_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_CERTIFICATION (
	CERTIFICATION_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	PART_ID VARCHAR(20) NOT NULL,
	FAA_APPROVED VARCHAR(1) DEFAULT 'N',
	EASA_APPROVED VARCHAR(1) DEFAULT 'N',
	AS9100 VARCHAR(1) DEFAULT 'N',
	ITAR VARCHAR(1) DEFAULT 'N',
	EXPORT_CONTROL VARCHAR(1) DEFAULT 'N',
	CERTIFICATION_DATE DATE,
	EXPIRY_DATE DATE,
	ISSUING_AUTHORITY VARCHAR(50),
	CERTIFICATE_NUMBER VARCHAR(30),
	AUDIT_NOTES VARCHAR(255),
	LAST_UPDATED TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	primary key (CERTIFICATION_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_CUSTOMER (
	CUSTOMER_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	CUSTOMER_ID VARCHAR(15) NOT NULL,
	CUSTOMER_CODE VARCHAR(30) NOT NULL,
	CUSTOMER_NAME VARCHAR(200) NOT NULL,
	COUNTRY VARCHAR(50),
	REGION VARCHAR(50),
	CUSTOMER_TYPE VARCHAR(30),
	REVENUE_TIER VARCHAR(20),
	ANNUAL_REVENUE NUMBER(18,2),
	CREDIT_LIMIT NUMBER(18,2),
	IS_ACTIVE BOOLEAN DEFAULT TRUE,
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (CUSTOMER_ID),
	primary key (CUSTOMER_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_PART cluster by (PART_FAMILY, ATA_CHAPTER)(
	PART_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	PART_ID VARCHAR(20) NOT NULL,
	PART_NUMBER VARCHAR(30) NOT NULL,
	PART_NAME VARCHAR(200) NOT NULL,
	PART_FAMILY VARCHAR(50),
	ATA_CHAPTER VARCHAR(50),
	CRITICALITY VARCHAR(30),
	UNIT_OF_MEASURE VARCHAR(10),
	STANDARD_COST NUMBER(18,4),
	WEIGHT_KG NUMBER(10,4),
	LEAD_TIME_DAYS NUMBER(38,0),
	MAKE_BUY_CODE VARCHAR(10),
	ITAR_CONTROLLED BOOLEAN DEFAULT FALSE,
	LIFECYCLE_STATUS VARCHAR(20) DEFAULT 'Active',
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (PART_ID),
	primary key (PART_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_PLANT cluster by (REGION, COUNTRY)(
	PLANT_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	PLANT_ID VARCHAR(10) NOT NULL,
	PLANT_CODE VARCHAR(10) NOT NULL,
	PLANT_NAME VARCHAR(200) NOT NULL,
	COUNTRY VARCHAR(50),
	REGION VARCHAR(50),
	CITY VARCHAR(100),
	PLANT_TYPE VARCHAR(30),
	CAPACITY_UNITS NUMBER(38,0),
	OPERATING_SHIFTS NUMBER(38,0) DEFAULT 2,
	SQUARE_METERS NUMBER(38,0),
	IS_ACTIVE BOOLEAN DEFAULT TRUE,
	LATITUDE NUMBER(9,6),
	LONGITUDE NUMBER(9,6),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (PLANT_ID),
	primary key (PLANT_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_RAW_MATERIAL (
	MATERIAL_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	MATERIAL_ID VARCHAR(15) NOT NULL,
	MATERIAL_CODE VARCHAR(30) NOT NULL,
	MATERIAL_NAME VARCHAR(200) NOT NULL,
	MATERIAL_TYPE VARCHAR(50),
	SPECIFICATION VARCHAR(100),
	UNIT_OF_MEASURE VARCHAR(10),
	STANDARD_COST NUMBER(18,4),
	DENSITY NUMBER(10,4),
	ITAR_CONTROLLED BOOLEAN DEFAULT FALSE,
	SHELF_LIFE_DAYS NUMBER(38,0),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (MATERIAL_ID),
	primary key (MATERIAL_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_ROUTING (
	ROUTING_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	ROUTING_ID VARCHAR(30) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	OPERATION_SEQ NUMBER(38,0) NOT NULL,
	WORK_CENTER_ID VARCHAR(15) NOT NULL,
	STANDARD_TIME_HRS NUMBER(10,4),
	SETUP_TIME_HRS NUMBER(10,4),
	DESCRIPTION VARCHAR(200),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (ROUTING_ID),
	primary key (ROUTING_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_SUPPLIER cluster by (TIER_LEVEL, COUNTRY)(
	SUPPLIER_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	SUPPLIER_ID VARCHAR(15) NOT NULL,
	SUPPLIER_CODE VARCHAR(30) NOT NULL,
	SUPPLIER_NAME VARCHAR(200) NOT NULL,
	TIER_LEVEL NUMBER(1,0) NOT NULL,
	COUNTRY VARCHAR(50),
	REGION VARCHAR(50),
	CITY VARCHAR(100),
	SUPPLIER_TYPE VARCHAR(50),
	CATEGORY VARCHAR(20),
	RISK_SCORE NUMBER(5,2),
	QUALITY_RATING NUMBER(5,2),
	ON_TIME_DELIVERY_PCT NUMBER(5,2),
	ANNUAL_REVENUE NUMBER(18,2),
	EMPLOYEE_COUNT NUMBER(38,0),
	CERTIFICATION_STATUS VARCHAR(30),
	ITAR_CONTROLLED BOOLEAN DEFAULT FALSE,
	EAR_CONTROLLED BOOLEAN DEFAULT FALSE,
	IS_ACTIVE BOOLEAN DEFAULT TRUE,
	EFFECTIVE_DATE DATE DEFAULT CURRENT_DATE(),
	EXPIRY_DATE DATE DEFAULT CAST('9999-12-31' AS DATE),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (SUPPLIER_ID),
	primary key (SUPPLIER_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_SUPPLIER_PART (
	SUPPLIER_PART_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	SUPPLIER_PART_ID VARCHAR(30) NOT NULL,
	SUPPLIER_ID VARCHAR(15) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	LEAD_TIME_DAYS NUMBER(38,0),
	MOQ NUMBER(38,0),
	CONTRACT_PRICE NUMBER(18,4),
	CERTIFICATION_STATUS VARCHAR(20),
	SUPPLIER_RATING NUMBER(5,2),
	CONTRACT_ID VARCHAR(20),
	EFFECTIVE_DATE DATE,
	EXPIRY_DATE DATE,
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (SUPPLIER_PART_ID),
	primary key (SUPPLIER_PART_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_WAREHOUSE (
	WAREHOUSE_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	WAREHOUSE_ID VARCHAR(15) NOT NULL,
	WAREHOUSE_CODE VARCHAR(30) NOT NULL,
	WAREHOUSE_NAME VARCHAR(200) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	WAREHOUSE_TYPE VARCHAR(30),
	CAPACITY_UNITS NUMBER(38,0),
	CURRENT_UTILIZATION_PCT NUMBER(5,2),
	IS_ACTIVE BOOLEAN DEFAULT TRUE,
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (WAREHOUSE_ID),
	primary key (WAREHOUSE_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.DIM_WORK_CENTER (
	WORK_CENTER_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	WORK_CENTER_ID VARCHAR(15) NOT NULL,
	WORK_CENTER_CODE VARCHAR(30) NOT NULL,
	WORK_CENTER_NAME VARCHAR(200) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	MACHINE_TYPE VARCHAR(50),
	CAPACITY_HOURS_DAY NUMBER(5,2) DEFAULT 16,
	HOURLY_RATE NUMBER(10,2),
	IS_ACTIVE BOOLEAN DEFAULT TRUE,
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (WORK_CENTER_ID),
	primary key (WORK_CENTER_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_AOG_EVENT (
	AOG_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	AOG_ID VARCHAR(30) NOT NULL,
	CUSTOMER_ID VARCHAR(15) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	EVENT_DATE DATE NOT NULL,
	DURATION_HOURS NUMBER(10,2),
	REVENUE_IMPACT NUMBER(18,2),
	ROOT_CAUSE VARCHAR(255),
	RESOLUTION_ACTION VARCHAR(255),
	SEVERITY_CODE VARCHAR(10),
	RESOLUTION_DATE DATE,
	MAINTENANCE_PROVIDER_ID VARCHAR(15),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (AOG_ID),
	primary key (AOG_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_INVENTORY cluster by (PLANT_ID, PART_ID)(
	INVENTORY_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	INVENTORY_ID VARCHAR(30) NOT NULL,
	INVENTORY_CODE VARCHAR(50) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	WAREHOUSE_ID VARCHAR(15) NOT NULL,
	ON_HAND_QTY NUMBER(18,2) DEFAULT 0,
	AVAILABLE_QTY NUMBER(18,2) DEFAULT 0,
	RESERVED_QTY NUMBER(18,2) DEFAULT 0,
	IN_TRANSIT_QTY NUMBER(18,2) DEFAULT 0,
	REORDER_POINT NUMBER(18,2),
	SAFETY_STOCK NUMBER(18,2),
	MAX_STOCK NUMBER(18,2),
	UNIT_COST NUMBER(18,4),
	TOTAL_VALUE NUMBER(18,2),
	LAST_RECEIPT_DATE DATE,
	LAST_ISSUE_DATE DATE,
	SNAPSHOT_DATE DATE DEFAULT CURRENT_DATE(),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (INVENTORY_ID),
	primary key (INVENTORY_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_INVENTORY_MOVEMENT cluster by (MOVEMENT_DATE, MOVEMENT_TYPE)(
	MOVEMENT_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	MOVEMENT_ID VARCHAR(30) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	WAREHOUSE_ID VARCHAR(15),
	MOVEMENT_TYPE VARCHAR(20) NOT NULL,
	QUANTITY NUMBER(18,2) NOT NULL,
	MOVEMENT_DATE DATE NOT NULL,
	BATCH_ID VARCHAR(20),
	LOT_NUMBER VARCHAR(20),
	TRANSACTION_ID VARCHAR(30),
	REASON_CODE VARCHAR(10),
	REFERENCE_DOC VARCHAR(30),
	CREATED_BY VARCHAR(15),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (MOVEMENT_ID),
	primary key (MOVEMENT_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_IOT_SENSOR_DATA cluster by (READING_TIMESTAMP, PLANT_ID)(
	SENSOR_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	SENSOR_ID VARCHAR(30) NOT NULL,
	WORK_CENTER_ID VARCHAR(15) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	READING_TIMESTAMP TIMESTAMP_NTZ(9) NOT NULL,
	TEMPERATURE_C NUMBER(8,2),
	VIBRATION_MM_S NUMBER(8,4),
	PRESSURE_BAR NUMBER(8,2),
	RPM NUMBER(10,2),
	POWER_KW NUMBER(8,2),
	OIL_LEVEL_PCT NUMBER(5,2),
	STATUS VARCHAR(20) DEFAULT 'Normal',
	ALERT_FLAG BOOLEAN DEFAULT FALSE,
	primary key (SENSOR_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_PURCHASE_ORDER cluster by (ORDER_DATE, SUPPLIER_ID)(
	PO_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	PO_ID VARCHAR(30) NOT NULL,
	PO_CODE VARCHAR(50) NOT NULL,
	SUPPLIER_ID VARCHAR(15) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	ORDER_DATE DATE NOT NULL,
	PROMISED_DATE DATE,
	RECEIVED_DATE DATE,
	STATUS VARCHAR(20),
	TOTAL_VALUE NUMBER(18,2),
	CURRENCY_CODE VARCHAR(3) DEFAULT 'USD',
	BUYER_ID VARCHAR(15),
	PRIORITY_CODE VARCHAR(10),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (PO_ID),
	primary key (PO_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_PURCHASE_ORDER_LINE cluster by (PO_ID)(
	PO_LINE_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	PO_LINE_ID VARCHAR(30) NOT NULL,
	PO_ID VARCHAR(30) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	QUANTITY_ORDERED NUMBER(18,2) NOT NULL,
	QUANTITY_RECEIVED NUMBER(18,2) DEFAULT 0,
	UNIT_PRICE NUMBER(18,4),
	LINE_VALUE NUMBER(18,2),
	PROMISED_DATE DATE,
	RECEIVED_DATE DATE,
	STATUS VARCHAR(20),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (PO_LINE_ID),
	primary key (PO_LINE_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_PURCHASE_REQUISITION (
	PR_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	PR_ID VARCHAR(30) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	REQUESTED_QTY NUMBER(18,2),
	NEED_DATE DATE,
	STATUS VARCHAR(20),
	SUPPLIER_ID VARCHAR(15),
	CURRENCY_CODE VARCHAR(3) DEFAULT 'USD',
	APPROVAL_DATE DATE,
	BUYER_ID VARCHAR(15),
	PRIORITY_CODE VARCHAR(10),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (PR_ID),
	primary key (PR_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_QUALITY_EVENT cluster by (EVENT_DATE, PLANT_ID)(
	QUALITY_EVENT_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	QUALITY_EVENT_ID VARCHAR(30) NOT NULL,
	QUALITY_EVENT_CODE VARCHAR(50) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	SUPPLIER_ID VARCHAR(15),
	PLANT_ID VARCHAR(10) NOT NULL,
	WORK_ORDER_ID VARCHAR(30),
	EVENT_TYPE VARCHAR(30),
	DEFECT_TYPE VARCHAR(50),
	SEVERITY VARCHAR(10),
	ROOT_CAUSE VARCHAR(200),
	CORRECTIVE_ACTION VARCHAR(200),
	DISPOSITION VARCHAR(20),
	QUANTITY_INSPECTED NUMBER(18,2),
	QUANTITY_DEFECTIVE NUMBER(18,2),
	EVENT_DATE DATE NOT NULL,
	RESOLUTION_DATE DATE,
	STATUS VARCHAR(20),
	COST_OF_QUALITY NUMBER(18,2),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (QUALITY_EVENT_ID),
	primary key (QUALITY_EVENT_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_REPAIR_ORDER (
	REPAIR_ORDER_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	REPAIR_ORDER_ID VARCHAR(30) NOT NULL,
	ENGINE_SN VARCHAR(30),
	PART_ID VARCHAR(20) NOT NULL,
	CUSTOMER_ID VARCHAR(15),
	RECEIVED_DATE DATE,
	RELEASED_DATE DATE,
	REPAIR_COST NUMBER(18,2),
	REPAIR_STATUS VARCHAR(20),
	TECHNICIAN_ID VARCHAR(15),
	REPAIR_TYPE VARCHAR(20),
	WARRANTY_FLAG VARCHAR(1) DEFAULT 'N',
	TURNAROUND_TIME_DAYS NUMBER(10,2),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (REPAIR_ORDER_ID),
	primary key (REPAIR_ORDER_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_SALES_ORDER cluster by (ORDER_DATE, CUSTOMER_ID)(
	SO_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	SO_ID VARCHAR(30) NOT NULL,
	SO_CODE VARCHAR(50) NOT NULL,
	CUSTOMER_ID VARCHAR(15) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	ORDER_DATE DATE NOT NULL,
	REQUESTED_DATE DATE,
	PROMISED_DATE DATE,
	SHIPPED_DATE DATE,
	STATUS VARCHAR(20),
	TOTAL_VALUE NUMBER(18,2),
	CURRENCY_CODE VARCHAR(3) DEFAULT 'USD',
	PRIORITY VARCHAR(10),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (SO_ID),
	primary key (SO_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_SALES_ORDER_LINE cluster by (SO_ID)(
	SO_LINE_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	SO_LINE_ID VARCHAR(30) NOT NULL,
	SO_ID VARCHAR(30) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	QUANTITY_ORDERED NUMBER(18,2) NOT NULL,
	QUANTITY_SHIPPED NUMBER(18,2) DEFAULT 0,
	UNIT_PRICE NUMBER(18,4),
	LINE_VALUE NUMBER(18,2),
	REQUESTED_DATE DATE,
	PROMISED_DATE DATE,
	STATUS VARCHAR(20),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (SO_LINE_ID),
	primary key (SO_LINE_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_SHIPMENT cluster by (SHIP_DATE, SHIPMENT_TYPE)(
	SHIPMENT_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	SHIPMENT_ID VARCHAR(30) NOT NULL,
	SHIPMENT_CODE VARCHAR(50) NOT NULL,
	SHIPMENT_TYPE VARCHAR(20) NOT NULL,
	ORIGIN_PLANT_ID VARCHAR(10),
	DESTINATION_PLANT_ID VARCHAR(10),
	SUPPLIER_ID VARCHAR(15),
	CUSTOMER_ID VARCHAR(15),
	CARRIER_ID VARCHAR(15),
	PO_ID VARCHAR(30),
	SO_ID VARCHAR(30),
	PART_ID VARCHAR(20),
	QUANTITY NUMBER(18,2),
	SHIP_DATE DATE,
	PROMISED_DELIVERY_DATE DATE,
	ACTUAL_DELIVERY_DATE DATE,
	STATUS VARCHAR(20),
	FREIGHT_COST NUMBER(18,2),
	WEIGHT_KG NUMBER(10,2),
	TRACKING_NUMBER VARCHAR(50),
	IS_ON_TIME BOOLEAN,
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (SHIPMENT_ID),
	primary key (SHIPMENT_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.FACT_WORK_ORDER cluster by (PLANNED_START_DATE, PLANT_ID)(
	WORK_ORDER_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	WORK_ORDER_ID VARCHAR(30) NOT NULL,
	WORK_ORDER_CODE VARCHAR(50) NOT NULL,
	WORK_ORDER_TYPE VARCHAR(20) NOT NULL,
	PART_ID VARCHAR(20) NOT NULL,
	PLANT_ID VARCHAR(10) NOT NULL,
	WORK_CENTER_ID VARCHAR(15),
	QUANTITY_ORDERED NUMBER(18,2) NOT NULL,
	QUANTITY_COMPLETED NUMBER(18,2) DEFAULT 0,
	QUANTITY_SCRAPPED NUMBER(18,2) DEFAULT 0,
	PLANNED_START_DATE DATE,
	PLANNED_END_DATE DATE,
	ACTUAL_START_DATE DATE,
	ACTUAL_END_DATE DATE,
	STATUS VARCHAR(20),
	PRIORITY VARCHAR(10),
	YIELD_RATE NUMBER(5,2),
	TOTAL_COST NUMBER(18,2),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	UPDATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (WORK_ORDER_ID),
	primary key (WORK_ORDER_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.META_BUSINESS_GLOSSARY (
	TERM_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	TERM_ID VARCHAR(15) NOT NULL,
	BUSINESS_TERM VARCHAR(100) NOT NULL,
	BUSINESS_DEFINITION VARCHAR(500),
	SOURCE_TABLE VARCHAR(100),
	FORMULA VARCHAR(1000),
	OWNER VARCHAR(100),
	STATUS VARCHAR(20) DEFAULT 'Active',
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (TERM_ID),
	primary key (TERM_SK)
);
create or replace TABLE AEROSPACE_SUPPLY_CHAIN.RAW.META_METRIC_DEFINITION (
	METRIC_SK NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	METRIC_ID VARCHAR(15) NOT NULL,
	METRIC_NAME VARCHAR(100) NOT NULL,
	METRIC_DESCRIPTION VARCHAR(500),
	FORMULA VARCHAR(2000),
	UNIT VARCHAR(20),
	TARGET_VALUE NUMBER(18,4),
	SOURCE_TABLES VARCHAR(500),
	GRAIN VARCHAR(200),
	BUSINESS_RULE VARCHAR(1000),
	CREATED_TIMESTAMP TIMESTAMP_NTZ(9) DEFAULT CURRENT_TIMESTAMP(),
	unique (METRIC_ID),
	primary key (METRIC_SK)
);